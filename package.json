{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "api-server",
    "client",
    "lib/*",
    "scripts"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w @workspace/api-server\" \"npm run dev -w @workspace/trail-finder\"",
    "build": "npm run typecheck && npm run build --workspaces --if-present",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck -w @workspace/api-server -w @workspace/trail-finder -w @workspace/mockup-sandbox -w @workspace/scripts --if-present",
    "import:trails": "npm run import:trails -w @workspace/scripts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "overrides": {
    "@esbuild-kit/esm-loader": "npm:tsx@^4.21.0"
  }
}
